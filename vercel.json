{
  "installCommand": "pnpm install --no-frozen-lockfile",
  "buildCommand": "cd artifacts/restaurant-site && pnpm run build",
  "outputDirectory": "artifacts/restaurant-site/dist/public",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
